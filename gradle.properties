org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Machine running the Firebase emulators, as seen from the device. This is the developer's LAN IP so a
# physical phone on the same Wi-Fi can sync scores. For the Android emulator use 10.0.2.2 instead, e.g.
#   ./gradlew :app:installDebug -PfirebaseEmulatorHost=10.0.2.2
# (or set firebaseEmulatorHost in local.properties).
firebaseEmulatorHost=192.168.178.86
